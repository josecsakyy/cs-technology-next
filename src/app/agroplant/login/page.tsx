"use client";import Image from "next/image";import Link from "next/link";import {FormEvent,useState} from "react";
export default function Login(){const[username,setUsername]=useState("AGROPLANT"),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");const r=await fetch("/api/agroplant/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username,password})});setLoading(false);if(r.ok)location.href="/agroplant";else setError((await r.json()).error);}return <main className="flex min-h-screen items-center justify-center bg-[#071710] px-5 text-white"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-white/10 bg-[#0c2117] p-8 shadow-2xl"><div className="flex items-center gap-4"><Image src="/brand/logo-cosechadora.png" alt="CS" width={64} height={64} className="rounded-full"/><div><p className="text-xs tracking-[.2em] text-emerald-300">ACCESO PRIVADO</p><h1 className="text-2xl font-semibold">Monitor Agroplant</h1></div></div><label className="mt-7 block text-sm text-white/70">Usuario</label><input value={username} onChange={e=>setUsername(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/25 px-4 py-3"/><label className="mt-4 block text-sm text-white/70">Contraseña</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/25 px-4 py-3"/>{error&&<p className="mt-4 text-sm text-red-300">{error}</p>}<button disabled={loading} className="mt-6 w-full rounded-xl bg-emerald-500 py-3 font-semibold text-[#062013]">{loading?"Ingresando…":"Ingresar al monitor"}</button><Link href="/" className="mt-5 block text-center text-sm text-white/50">Volver al sitio</Link></form></main>}
